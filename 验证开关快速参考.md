# 验证开关快速参考

## 🚀 快速使用

### 打包无验证版本
```bash
npm run dist:no-license:win    # Windows
npm run dist:no-license:mac    # Mac
npm run dist:no-license        # 所有平台
```

### 打包有验证版本（默认）
```bash
npm run dist:win    # Windows
npm run dist:mac    # Mac
npm run dist        # 所有平台
```

## 🔧 工作原理

**环境变量：** `DISABLE_LICENSE_CHECK`

**设置方式：**
- `DISABLE_LICENSE_CHECK=true` → 禁用验证
- `DISABLE_LICENSE_CHECK=1` → 禁用验证
- 未设置或 `false` → 启用验证（默认）

**代码位置：** `src/main/license-service.ts` 第 44 行

## ✅ 效果

**禁用验证时：**
- ✅ 无需激活卡密
- ✅ 直接使用所有功能
- ✅ 不会显示卡密激活界面
- ✅ `getLicenseStatus()` 返回 `{ valid: true }`

**启用验证时（默认）：**
- ✅ 需要激活卡密
- ✅ 启动时检查许可证
- ✅ 验证失败会要求激活

## 📝 注意事项

1. **环境变量需要在构建时设置**
2. **使用 `cross-env` 确保跨平台兼容**
3. **已添加到 devDependencies，运行 `npm install` 即可**

