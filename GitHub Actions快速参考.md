# GitHub Actions 快速参考

## 🚀 快速开始

### 方法一：推送代码自动构建
```bash
git add .
git commit -m "Update"
git push origin main
```
→ 自动触发构建 Mac 和 Windows 版本

### 方法二：手动触发构建
1. 打开 GitHub 仓库
2. 点击 **Actions** 标签页
3. 选择 **Build All Platforms**
4. 点击 **Run workflow**
5. 选择分支和选项
6. 点击 **Run workflow**

### 方法三：创建版本发布
```bash
git tag v1.0.4
git push origin v1.0.4
```
→ 自动构建并创建 GitHub Release

## 📦 下载构建产物

**构建完成后：**
1. 进入 **Actions** 标签页
2. 点击最新的工作流运行
3. 在 **Artifacts** 部分下载：
   - `mac-build-{编号}` - Mac 版本（.dmg, .zip）
   - `win-build-{编号}` - Windows 版本（.exe）

## ⚙️ 构建选项

**有验证版本（默认）：**
- 需要激活卡密
- 不勾选 "Build without license check"

**无验证版本：**
- 无需卡密
- 勾选 "Build without license check"

## ⏱️ 构建时间

- Mac: ~5-10 分钟
- Windows: ~5-10 分钟
- 总计: ~10-20 分钟（并行构建）

## 📝 工作流文件

- `.github/workflows/build-all.yml` - 主工作流（推荐）
- `.github/workflows/build.yml` - 备用工作流

