# Cursor 账号管理器

一个跨平台的桌面应用，用于管理多个Cursor Pro账号，支持一键切换账号、重置机器码、会话备份等功能。

## ✨ 核心功能

- 🔄 **快速切换账号** - 一键切换多个Cursor Pro账号
- 🔑 **Token注入** - 直接修改Cursor的认证数据库，无需复制文件
- 🖥️ **机器码重置** - 重置设备标识，避免封禁风险
- 🔥 **恢复出厂设置** - 彻底重置Cursor到全新状态
- 💾 **会话备份** - 备份和恢复Cursor会话、工作区、历史记录
- ⚙️ **设置备份** - 备份和恢复编辑器设置、快捷键、代码片段
- 📥 **快速导入** - 一键从Cursor导入当前账号
- 🎨 **现代化界面** - 美观易用的图形界面
- 🚀 **跨平台支持** - 支持 macOS 和 Windows
- 🔐 **卡密激活** - 支持卡密激活和自动账号注入

## 🛠️ 技术栈

- **前端**: React 18 + TypeScript + Tailwind CSS
- **后端**: Electron 28
- **数据库**: SQLite3 (better-sqlite3)
- **构建**: Vite + electron-vite
- **打包**: electron-builder
- **代码质量**: ESLint + Prettier + TypeScript
- **日志**: electron-log

## 📦 安装依赖

```bash
npm install
```

## ⚙️ 环境变量配置

为了保护敏感信息（如后端服务地址），项目使用环境变量管理配置。

### 首次配置

1. 复制环境变量示例文件：
```bash
cp .env.example .env
```

2. 编辑 `.env` 文件，填入实际值：
```bash
# 许可证服务器地址（必填）
LICENSE_SERVER_URL=http://your-server-ip:8080

# 许可证公钥（必填）
LICENSE_PUBLIC_KEY=your-public-key-base64
```

⚠️ **重要提示**：
- `.env` 文件已添加到 `.gitignore`，不会被提交到Git仓库
- 不要将 `.env` 文件提交到公开仓库
- `.env.example` 是模板文件，可以提交到Git

## 🚀 开发运行

```bash
npm run dev
```

## 📱 打包应用

### Mac版本（在 Mac 上）
```bash
npm run build
npm run dist:mac
```

### Windows版本（推荐在 Windows 上）
```bash
# 1. 诊断构建环境（可选）
npm run diagnose

# 2. 清理旧文件（可选）
npm run clean

# 3. 安装依赖
npm install

# 4. 构建和打包
npm run build
npm run dist:win
```

**⚠️ Windows 构建重要提示**

如果 Windows 版本无法启动，通常是 `better-sqlite3` 原生模块的问题：

**推荐方案（在 Windows 机器上构建）**：
```bash
# 首次需要安装构建工具
npm install --global windows-build-tools

# 清理、安装、构建
npm run clean
npm install
npm run build
npm run dist:win
```

**快速诊断**：
```bash
npm run diagnose  # 自动检查构建环境和依赖
```

**使用 GitHub Actions 自动构建**：
- 已配置自动在 Windows/Mac 平台构建
- 推送代码后在 Actions 标签页下载构建产物

**常见问题**：
- 应用无法启动：必须在 Windows 上重新 `npm install` 和构建
- 找不到模块：运行 `npm run rebuild:electron`
- 架构不匹配：在目标平台上重新构建

## 🔧 开发工具

### 代码质量检查
```bash
npm run lint          # 检查代码质量
npm run lint:fix      # 自动修复问题
npm run type-check    # TypeScript类型检查
npm run format        # 格式化代码
npm run format:check  # 检查格式
```

## 📖 使用说明

### 1. 添加账号

点击"添加账号"按钮，填写以下信息：
- **邮箱地址**: Cursor账号的邮箱
- **Access Token**: 从Cursor配置中获取的token

#### 如何获取Token？

**方法一：手动提取**

Mac系统：
```bash
sqlite3 "~/Library/Application Support/Cursor/User/globalStorage/state.vscdb" \
  "SELECT value FROM ItemTable WHERE key='cursorAuth/accessToken'"
```

Windows系统：
```cmd
sqlite3 "%APPDATA%\Cursor\User\globalStorage\state.vscdb" \
  "SELECT value FROM ItemTable WHERE key='cursorAuth/accessToken'"
```

**方法二：使用本工具导入**
- 在Cursor中登录账号
- 在本工具中点击"导入当前账号"

### 2. 切换账号

1. 在账号列表中选择要切换的账号
2. 点击"切换"按钮
3. 工具会自动关闭Cursor并注入新的Token
4. 根据设置自动重启Cursor或手动启动

### 3. 重置机器码

当频繁切换账号导致被限制时：
1. 点击"重置机器码"按钮
2. 工具会生成新的设备标识
3. Cursor会被识别为新设备

⚠️ **注意**: 重置机器码会清除当前登录状态，需要重新登录

### 4. 深度重置（跨平台）

执行更彻底的系统级重置：
1. 点击"🔥🔥 深度重置 🔥🔥"按钮
2. 会执行：

**Mac 系统：**
   - 修改系统UUID
   - 清除DNS缓存
   - 修改Cursor程序文件
   - 移除并重新签名应用

**Windows 系统：**
   - 修改注册表 MachineGuid（需要管理员权限）
   - 清除DNS缓存
   - 清除网络缓存（ARP、NetBIOS）
   - 修改Cursor程序文件

⚠️ **重要提示**:
- Windows需要以**管理员身份**运行才能完整执行深度重置
- Mac上首次启动修改后的Cursor可能需要在"系统偏好设置→安全性"中允许
- Windows上首次运行可能触发SmartScreen警告，点击"更多信息"→"仍要运行"即可
- 建议深度重置后重启计算机

### 5. 备份和恢复

#### 会话备份
备份内容包括：
- ✅ state.vscdb（会话状态和认证信息）
- ✅ storage.json（机器码和配置）
- ✅ workspaceStorage（工作区数据）
- ✅ History（历史记录）
- ✅ Cookies（认证Cookie）
- ✅ Local Storage（本地存储）

#### 设置备份
备份内容包括：
- ✅ settings.json（编辑器设置）
- ✅ keybindings.json（快捷键配置）
- ✅ snippets（代码片段）
- ✅ profiles.json（配置文件）
- ✅ extensions.json（扩展列表）

**使用场景：**
- 切换账号前备份当前会话
- 保存特定账号的完整状态
- 在不同设备间迁移会话
- 重装系统后快速恢复配置

## 🔐 卡密激活功能

### 功能说明

独占卡密功能让每个卡密都绑定一个独立的 Cursor Token，实现：
- ✅ **一卡密一账号**：每个卡密对应独立的 Cursor 账号
- ✅ **自动注入登录**：输入卡密后自动注入 Cursor 账号，无需手动登录
- ✅ **简单快捷**：只需输入卡密，软件自动完成所有配置

### 使用流程

#### 首次使用
软件启动时会自动检查是否有卡密，如果没有会弹出卡密输入框：

1. 输入你的卡密
2. 点击"激活并注入账号"
3. 等待激活成功
4. 启动 Cursor，会自动登录到对应账号

#### 更换卡密
在侧边栏底部，点击黄色的 **🔑 更换卡密** 按钮：

1. 点击"更换卡密"按钮
2. 如果 Cursor 正在运行，会提示关闭
3. 输入新的卡密
4. 点击"激活并注入账号"
5. 启动 Cursor，自动登录到新账号

#### 解绑设备
如果需要更换设备，可以在更换卡密界面点击"解绑此设备"

### 常见问题

**Q: 为什么需要关闭 Cursor？**  
A: 注入账号信息需要修改 Cursor 的数据库文件，必须在 Cursor 关闭时才能安全操作。

**Q: 更换卡密后之前的账号还能用吗？**  
A: 更换卡密后，软件会自动切换到新卡密对应的账号，之前的卡密不会自动失效，但软件只使用最新的卡密。

**Q: 可以在多台设备上使用同一个卡密吗？**  
A: 取决于卡密的设备数限制。如果卡密支持多设备，可以在多台设备上激活。

**Q: 卡密过期了怎么办？**  
A: 卡密过期后软件会提示需要重新输入卡密，使用新卡密即可。

## 🔒 安全说明

- 所有账号数据存储在本地SQLite数据库中
- 不会上传任何数据到远程服务器
- Token数据仅用于本地切换账号
- 卡密信息加密存储

## 📂 数据存储位置

### Mac
- 应用数据: `~/Library/Application Support/cursor-account-manager/`
- 数据库: `~/Library/Application Support/cursor-account-manager/accounts.db`
- 日志文件: `~/Library/Application Support/cursor-account-manager/logs/main.log`
- 备份目录: `~/Library/Application Support/cursor-account-manager/backups/`
- Cursor配置: `~/Library/Application Support/Cursor/`

### Windows
- 应用数据: `%APPDATA%\cursor-account-manager\`
- 数据库: `%APPDATA%\cursor-account-manager\accounts.db`
- 日志文件: `%APPDATA%\cursor-account-manager\logs\main.log`
- 备份目录: `%APPDATA%\cursor-account-manager\backups\`
- Cursor配置: `%APPDATA%\Cursor\`

## ⚙️ 工作原理

### Token注入机制

1. Cursor将认证信息存储在 `state.vscdb` SQLite数据库中
2. 关键字段：
   - `cursorAuth/accessToken` - 访问令牌
   - `cursorAuth/refreshToken` - 刷新令牌
   - `cursorAuth/cachedEmail` - 账号邮箱
3. 本工具直接修改数据库中的这些字段实现账号切换

### 机器码重置

修改 `storage.json` 中的以下字段：
- `telemetry.machineId` - 主机器码
- `telemetry.macMachineId` - MAC机器码
- `telemetry.devDeviceId` - 设备ID
- `telemetry.sqmId` - SQM ID

## 🐛 常见问题

### Q: 切换账号后Cursor无法启动？
A: 确保在切换前完全关闭了Cursor，可以尝试：
1. 手动强制关闭Cursor进程
2. 等待3-5秒后再启动

### Q: Token失效怎么办？
A: 
1. 在Cursor中重新登录该账号
2. 使用"导入当前账号"功能更新Token

### Q: 重置机器码后仍然被限制？
A: 
1. 检查是否完全清除了缓存
2. 尝试重新安装Cursor
3. 等待一段时间后再尝试

### Q: 如何查看日志？
A: 日志文件位置：
- Mac: `~/Library/Application Support/cursor-account-manager/logs/main.log`
- Windows: `%APPDATA%\cursor-account-manager\logs\main.log`

## 📝 更新日志

### v1.0.2 (2024-10-28)

#### 💾 新增功能 - 完整的备份和恢复系统

**会话备份和恢复**
- ✅ state.vscdb（会话状态和认证信息）
- ✅ storage.json（机器码和配置）
- ✅ workspaceStorage（工作区数据）
- ✅ History（历史记录）
- ✅ Cookies（认证Cookie）
- ✅ Local Storage（本地存储）

**设置备份和恢复**
- ✅ settings.json（编辑器设置）
- ✅ keybindings.json（快捷键配置）
- ✅ snippets（代码片段）
- ✅ profiles.json（配置文件）
- ✅ extensions.json（扩展列表）

**备份管理界面**
- 🎨 新增"备份管理"标签页
- 📋 列出所有备份，显示类型、时间、账号
- 🔄 一键恢复备份
- 🗑️ 删除不需要的备份

### v1.0.1 (2024-10-28)

#### 🔥 新增功能

**完整恢复出厂设置**
- ✅ 重置机器码（生成全新设备ID）
- ✅ 清除所有认证信息（登出）
- ✅ 清空所有缓存目录
- ✅ 清空Local Storage和Session Storage
- ✅ 清空工作区历史和历史记录

**快速导入账号**
- 📥 一键从Cursor导入当前账号
- 自动读取邮箱和token

### v1.0.0 (2024-10-28)

#### ✨ 首次发布

**核心功能：**
- ✅ 多账号管理（增删改查）
- ✅ Token直接注入切换
- ✅ 机器码重置
- ✅ 自动关闭/启动Cursor
- ✅ 现代化UI界面
- ✅ 跨平台支持（Mac/Windows）

## 🎯 项目优化

### ✅ 已完成的优化

1. **package.json 完善**
   - ✅ 更新了 `author` 字段
   - ✅ 添加了 `repository`、`keywords`、`engines` 字段
   - ✅ 添加了代码质量检查脚本

2. **统一日志系统**
   - ✅ 创建了 `src/main/logger.ts` 模块
   - ✅ 使用 `electron-log` 统一管理日志
   - ✅ 支持不同的日志级别（debug/info/warn/error）

3. **配置管理优化**
   - ✅ 创建了 `src/main/config.ts` 模块
   - ✅ 支持环境变量覆盖配置
   - ✅ 支持数据库配置存储

4. **统一错误处理**
   - ✅ 创建了 `src/main/error-handler.ts` 模块
   - ✅ 定义了 `AppError` 错误类
   - ✅ 提供了统一的错误处理函数

5. **代码质量工具**
   - ✅ 添加了 ESLint 和 Prettier 配置
   - ✅ 配置了代码格式化规则

6. **CI/CD 改进**
   - ✅ 更新了 GitHub Actions，添加了 lint 和 type-check 步骤

### 📝 开发者指南

#### 日志使用示例
```typescript
import { logger } from './logger'

logger.debug('调试信息', data)
logger.info('普通信息')
logger.warn('警告信息')
logger.error('错误信息', error)
```

#### 配置使用示例
```typescript
import { getConfig, updateConfig } from './config'

// 获取配置
const config = getConfig()
const serverUrl = config.license.serverUrl

// 更新配置
updateConfig({
  license: {
    serverUrl: 'https://new-server.com',
  },
})
```

#### 错误处理示例
```typescript
import { handleIpcError, AppError, ErrorCode } from './error-handler'

// 在IPC handler中使用
ipcMain.handle('someAction', async () => {
  try {
    // ... 业务逻辑
    return { success: true }
  } catch (error) {
    return handleIpcError(error)
  }
})
```

## 📄 许可证

MIT License

## 👨‍💻 贡献

欢迎提交Issue和Pull Request！

### 贡献指南

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范

- 使用 TypeScript 编写代码
- 遵循 ESLint 和 Prettier 配置
- 提交前运行 `npm run lint` 和 `npm run type-check`
- 使用统一的日志模块记录日志
- 使用统一的错误处理模块处理错误

## ⚖️ 免责声明

本工具仅供学习和研究使用，请遵守Cursor的服务条款。频繁切换账号可能违反服务协议，请谨慎使用。

## 📞 技术支持

如遇问题，请联系：
- GitHub Issues: [提交Issue](https://github.com/fengshao1227/cursor-tool/issues)
- 仓库地址: https://github.com/fengshao1227/cursor-tool.git

---

**祝使用愉快！** 🎉
